# Variables pour Docker compose
POSTGRES_USER=user
POSTGRES_PASSWORD=password
POSTGRES_DB=watchlist

# Base de données
DATABASE_URL="postgresql://user:password@localhost:5432/watchlist?schema=public"

# JWT
JWT_SECRET="super-secret-jwt-key-for-dev"
JWT_EXPIRES_IN="24h"

# Email (SMTP) - Configuration pour maildev
SMTP_HOST="localhost"
SMTP_PORT="1025"
SMTP_USER=""
SMTP_PASS=""
SMTP_FROM="no-reply@watchlist.app"

# Application
PORT="3000"
NODE_ENV="development"
FRONTEND_URL="http://localhost:3000"                                                                                                                                                                                      